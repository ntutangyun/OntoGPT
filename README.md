# Domain Knowledge Distillation Assistant

## Requirement
* Node.JS

## To start
Open up a terminal:
```bash
# install the yarn tool
npm install -g yarn

# use yarn to install dependencies
yarn install

# start the server
yarn start
```